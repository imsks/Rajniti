/**
 * App paths used for auth and onboarding redirects.
 * Keep in sync with middleware matcher and NextAuth callbacks.
 */
export const ROUTES = {
  dashboard: '/dashboard',
  onboarding: '/onboarding',
} as const
